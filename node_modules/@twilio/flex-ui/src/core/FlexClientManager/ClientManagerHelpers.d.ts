/**
 * Enum holding the list of Flex Clients to be used by the ClientManagerHelpers
 *
 * @enum
 * @property {"FlexSDKClient"} FlexSDKClient Flex SDK Client
 * @property {"TaskRouterClient"} TaskRouterClient TaskRouter Client
 * @property {"ConversationsClient"} ConversationsClient Conversations Client
 * @property {"VoiceClient"} VoiceClient Voice Client
 * @property {"InsightsClient"} InsightsClient Insights Client
 * @property {"PluginManager"} PluginManager Plugin Manager
 * @property {"FlexFeatures"} FlexFeatures Flex Features
 * @memberof ClientManagerHelpers
 */
export declare enum ClientManagerInstance {
    FlexSDKClient = "FlexSDKClient",
    TaskRouterClient = "TaskRouterClient",
    ConversationsClient = "ConversationsClient",
    VoiceClient = "VoiceClient",
    InsightsClient = "InsightsClient",
    PluginManager = "PluginManager",
    FlexFeatures = "FlexFeatures",
    CBMClient = "CBMClient"
}
/**
 * @category Overview
 * @class ClientManagerHelpers
 * @menuorder 300
 * @classdesc Flex Client Manager Helpers. Set of helpers to control Flex degraded mode behaviours.
 * @hideconstructor
 */
export declare class ClientManagerHelpers {
    private static forcedClientsMap;
    private static forceDegradedEnabled;
    private static getSDKClient;
    /**
     * Force a Flex Client to work in a degraded mode
     * Flex clients must be set degraded before the UI initialises
     *
     * @param {ClientManagerInstance} client The Flex Client instance
     * @param {boolean} [status] The degraded mode status. When set to `false`, it will cause Flex ro re-load.
     * @returns {Promise<void>}
     * @example
     * import { ClientManagerHelpers, ClientManagerInstance } from "@twilio/flex-ui";
     * ClientManagerHelpers.forceDegraded(ClientManagerInstance.VoiceClient);
     */
    static forceDegraded(client: ClientManagerInstance, status?: boolean): Promise<void>;
    /**
     * Return the force degraded status for a given Flex Client
     *
     * @param {ClientManagerInstance} client The Flex Client instance
     * @returns {boolean} Check if the client is forced degraded
     * @example
     * import { ClientManagerHelpers, ClientManagerInstance } from "@twilio/flex-ui";
     * ClientManagerHelpers.isForcedDegraded(ClientManagerInstance.VoiceClient);
     */
    static isForcedDegraded(client?: ClientManagerInstance): boolean;
}

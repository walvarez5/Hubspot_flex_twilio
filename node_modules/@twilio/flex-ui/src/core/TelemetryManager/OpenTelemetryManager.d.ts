interface TelemetryContext {
    traceId: string;
    spanId: string;
}
export default class OpenTelemetryManager {
    private readonly contextMap;
    constructor();
    private startSpan;
    private getSpanContext;
    getTelemetryContext(tracerName: string, spanName: string): TelemetryContext;
    endTelemetrySpan(spanName: string): void;
}
export {};

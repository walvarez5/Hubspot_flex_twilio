import MarkdownComponent from "markdown-to-jsx";
import * as React from "react";
type MarkdownToJsxProps = React.ComponentProps<typeof MarkdownComponent>;
interface MarkdownProps extends MarkdownToJsxProps {
    enableImages?: boolean;
    disableParsingRawHTML?: boolean;
    overrides?: Record<string, any>;
}
export declare const Markdown: React.FC<MarkdownProps>;
export {};

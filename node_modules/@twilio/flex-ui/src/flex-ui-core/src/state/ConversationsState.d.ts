import * as ConversationState from "./ConversationState";
/**
 * Conversations state
 *
 * @category State
 * @typedef {object} ConversationsState
 * @property {ConversationState} [sid] State of conversations by sid
 * @memberof AppState
 */
export interface ConversationsState extends Record<string, ConversationState.ConversationState> {
}
export declare function reduce(state: ConversationsState, action: ConversationState.ConversationAction): ConversationsState;

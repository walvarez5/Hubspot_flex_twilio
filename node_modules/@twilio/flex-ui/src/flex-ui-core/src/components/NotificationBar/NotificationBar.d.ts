import * as React from "react";
import { Notification } from "../../NotificationManager";
import { NotificationBarAction } from "./NotificationBarAction";
interface NotificationBarProps {
    filter: (notification: Notification) => boolean;
}
/**
 * @classdesc This component is used to render the notification bar
 * @class NotificationBar
 * @component
 * @category Components / Basic
 * @hideconstructor
 * @private
 */
export declare class NotificationBar extends React.PureComponent<NotificationBarProps> {
    /**
     * Notification bar action component
     *
     * @static
     * @type {NotificationBarAction}
     * @readonly
     */
    static readonly Action: typeof NotificationBarAction;
    static readonly defaultProps: NotificationBarProps;
    private onListenerHandler;
    componentDidMount(): void;
    componentWillUnmount(): void;
    render(): JSX.Element;
}
export {};

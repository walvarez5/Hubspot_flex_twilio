import * as React from "react";
interface CloseIconProps {
    onClick: () => void;
    inNotificationBar?: boolean;
}
export declare class CloseIcon extends React.PureComponent<CloseIconProps> {
    render(): JSX.Element;
}
export {};

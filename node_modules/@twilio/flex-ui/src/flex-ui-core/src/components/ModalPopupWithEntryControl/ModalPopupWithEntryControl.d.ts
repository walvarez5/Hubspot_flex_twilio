import * as React from "react";
export interface ModalPopupWithEntryControlProps {
    isOpen?: boolean;
    alignRight?: boolean;
    autoClose?: boolean;
    entryControl: React.ReactElement<any> | ((isModalOpen: boolean) => React.ReactElement<any>);
    onChange?: React.Dispatch<React.SetStateAction<boolean>>;
    className?: string;
    innerRef?: React.RefObject<ModalPopupWithEntryControl>;
}
export declare class ModalPopupWithEntryControl extends React.PureComponent<ModalPopupWithEntryControlProps> {
    private entryButtonRef;
    private modalPopup;
    render(): JSX.Element;
    private onEntryButtonBlur;
    private onModalBlur;
    private setEntryControlRef;
    private setModalPopupRef;
    static defaultProps: {
        className: string;
    };
    private getEntryControl;
}

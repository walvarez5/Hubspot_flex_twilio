import * as React from "react";
import { Notification } from "../../NotificationManager";
interface ScreenReaderAlertProps {
    filter: (notification: Notification) => boolean;
}
/**
 * @classdesc This component is used for the screen reader alert functionality
 * @class ScreenReaderAlert
 * @component
 * @category Components / Basic
 * @hideconstructor
 * @private
 */
export declare class ScreenReaderAlert extends React.Component<ScreenReaderAlertProps> {
    static readonly defaultProps: ScreenReaderAlertProps;
    private onListenerHandler;
    componentDidMount(): void;
    componentWillUnmount(): void;
    private renderMessageContent;
    render(): JSX.Element;
}
export {};

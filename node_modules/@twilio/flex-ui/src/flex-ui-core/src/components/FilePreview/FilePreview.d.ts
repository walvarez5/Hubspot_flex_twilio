import * as React from "react";
interface FilePreviewProps {
    file: Partial<File>;
    iconOverride?: React.ReactNode;
    titleLine?: React.ReactNode;
    subTitleLine?: React.ReactNode;
    disabled?: boolean;
    isBubble?: boolean;
    isFromMe?: boolean;
}
export declare class FilePreview extends React.PureComponent<FilePreviewProps> {
    renderIcon(): JSX.Element;
    customRounding(value: number): number;
    readableFileSize(fileSize: undefined | number): string;
    render(): JSX.Element;
}
export {};

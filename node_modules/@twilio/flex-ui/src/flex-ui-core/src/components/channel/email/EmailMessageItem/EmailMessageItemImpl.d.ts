import { Media } from "@twilio/conversations";
import { DynamicContentStore } from "../../../../DynamicComponent";
import { MessageState } from "../../../../state/ConversationState";
import { EmailMessageItemImplProps } from "./EmailMessageItem.definitions";
export declare const Body: ({ message, conversationSid, attachedMedia }: {
    message: MessageState;
    conversationSid: string;
    attachedMedia: Media[];
}) => JSX.Element;
export declare const EmailMessageItemImpl: (props: EmailMessageItemImplProps) => JSX.Element;
export declare const displayName = "EmailMessageItem";
export declare const Content: DynamicContentStore<any, "">;

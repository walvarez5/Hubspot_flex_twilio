import { ReactElement } from "react";
export interface EmailAccordionProps {
    header: ReactElement<WithIsOpenProp>;
    body: ReactElement<WithIsOpenProp>;
    defaultOpen?: boolean;
    strongBackgroundHeader?: boolean;
    onToggle?: (isOpening: boolean, isInitialOpening: boolean) => void;
    accordionId?: string;
}
export interface WithIsOpenProp {
    isOpen?: boolean;
}

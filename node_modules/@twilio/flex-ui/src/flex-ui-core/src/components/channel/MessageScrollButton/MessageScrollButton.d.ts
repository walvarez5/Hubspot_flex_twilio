import * as React from "react";
import { MessageState } from "../../../state/ConversationState";
export interface MessageScrollButtonProps {
    unreadMessages: MessageState[];
    onClick?: React.MouseEventHandler<HTMLButtonElement>;
}
/**
 * This component display the new unread message that displayed when the user scroll above the new separator.
 *
 * @component
 * @category Components / Basic
 * @hideconstructor
 * @param {MessageScrollButtonProps} props
 * @private
 */
export declare class MessageScrollButton extends React.PureComponent<MessageScrollButtonProps> {
    static readonly displayName = "MessageScrollButton";
    render(): JSX.Element;
    private getContent;
}

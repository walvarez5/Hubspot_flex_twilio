import { SPEditor } from "@udecode/plate-core";
/**
 * Unwrap link at a location (default: selection).
 * Then, the focus of the location is set to selection focus.
 * Then, wrap the link at the location.
 * @param {SPEditor} editor Plate Editor object
 * @param {object} root0 Options object
 * @param {string} root0.url url for Link
 * @param {boolean} root0.wrap wrap option
 * @param {boolean} root0.skipValidation skip validation option
 * @example upsertLinkAtSelection(editor, options)
 * @returns {void}
 */
export declare const upsertLinkAtSelection: (editor: SPEditor, { url, wrap, skipValidation }: {
    url: string;
    wrap?: boolean;
    skipValidation?: boolean;
}) => void;

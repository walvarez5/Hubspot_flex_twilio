import * as React from "react";
import { DynamicContentStore } from "../../../DynamicComponent";
import { MessageListItemChildrenKeys, MessageListItemProps } from "./MessageListItem.definitions";
export interface MediaErrorContainerProps {
    isFromMe: boolean;
}
export declare const displayName = "MessageListItem";
export declare const store: DynamicContentStore<MessageListItemProps, MessageListItemChildrenKeys>;
export declare class MessageListItemImpl extends React.PureComponent<MessageListItemProps> {
    renderErrorStatus(): JSX.Element;
    state: {
        isActiveElement: boolean;
    };
    private messageRef;
    private handleFocus;
    private handleBlur;
    private handleKeyDown;
    componentDidUpdate(newProps: MessageListItemProps): void;
    render(): JSX.Element;
}

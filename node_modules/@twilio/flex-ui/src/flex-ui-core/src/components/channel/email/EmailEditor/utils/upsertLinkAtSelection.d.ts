import { SPEditor } from "@udecode/plate-core";
import { Location } from "slate";
export declare const upsertLinkAtSelection: (editor: SPEditor, { url, wrap, text }: {
    url: string;
    wrap?: boolean;
    text?: string;
}) => Location | null;
export declare const removeLinkFromSelection: (editor: SPEditor) => void;

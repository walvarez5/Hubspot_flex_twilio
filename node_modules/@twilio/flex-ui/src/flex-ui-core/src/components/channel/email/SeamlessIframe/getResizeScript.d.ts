import { SeamlessIframeProps } from "./definitions";
export declare const renderResizeScript: (id: number, props: Pick<SeamlessIframeProps, "heightCorrection" | "heightCorrectionOnResize" | "debounceResizeTime">) => string;

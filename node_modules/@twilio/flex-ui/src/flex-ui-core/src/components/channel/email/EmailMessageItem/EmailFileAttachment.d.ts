import * as React from "react";
import { EmailFileAttachmentProps } from "./EmailMessageItem.definitions";
interface EmailFileAttachmentState {
    hover: boolean;
}
export declare class EmailFileAttachment extends React.PureComponent<EmailFileAttachmentProps, EmailFileAttachmentState> {
    state: {
        hover: boolean;
    };
    private downloadMedia;
    private toggleHover;
    private getDataStatus;
    render(): JSX.Element;
}
export {};

import { VirtualAgentDataSentences } from "@twilio/flex-sdk";
import { TranscriptMessageStyle } from "../TranscriptMessagingCanvas/TranscriptMessagingCanvasImpl";
/**
 * A object properties representing a message in message list.
 *
 * @typedef TranscriptMessageListItem.TranscriptMessageListItemProps
 * @property {VirtualAgentDataSentences} message - An object which represents a single message.
 * @property {TranscriptMessageStyle} messageStyle - Style of the transcript message bubble.
 * @private
 */
export interface TranscriptMessageListItemProps {
    message: VirtualAgentDataSentences;
    messageStyle: TranscriptMessageStyle;
}

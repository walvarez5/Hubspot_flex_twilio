import { Media } from "@twilio/conversations";
import * as React from "react";
import { MessageBubbleProps } from "./MessageBubble.definitions";
interface MessageBubbleMediaState {
    hover: boolean;
}
export declare class MessageBubbleMedia extends React.PureComponent<Pick<MessageBubbleProps, "message" | "conversationSid" | "inFocus"> & {
    media: Media;
}, MessageBubbleMediaState> {
    state: {
        hover: boolean;
    };
    private downloadMedia;
    private toggleHover;
    private getDataStatus;
    render(): JSX.Element;
}
export {};

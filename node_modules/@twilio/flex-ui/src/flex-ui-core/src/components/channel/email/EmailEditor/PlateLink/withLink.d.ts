import { SPEditor, WithOverride } from "@udecode/plate-core";
import { ReactEditor } from "slate-react";
import { WithLinkOptions } from "./types";
/**
 * Insert space after a url to wrap a link.
 * Lookup from the block start to the cursor to check if there is an url.
 * If not found, lookup before the cursor for a space character to check the url.
 *
 * On insert data:
 * Paste a string inside a link element will edit its children text but not its url.
 * @param {object} root0 Options object
 * @param {Function} root0.isUrl isUrl function override option
 * @param {WithLinkOptions.RangeBeforeOptions} root0.rangeBeforeOptions rangeBefore option
 * @param {string[]} root0.allowedSchemes allowedSchemes option
 * @example withLink(options)
 * @returns {Function} callback with editor parameter
 */
export declare const withLink: ({ isUrl, rangeBeforeOptions, allowedSchemes }?: WithLinkOptions) => WithOverride<ReactEditor & SPEditor>;

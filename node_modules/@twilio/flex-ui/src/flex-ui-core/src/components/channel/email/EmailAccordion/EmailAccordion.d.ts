import { EmailAccordionProps } from "./EmailAccordion.definitions";
export declare const EmailAccordion: {
    ({ header, defaultOpen, body, strongBackgroundHeader, accordionId, onToggle }: EmailAccordionProps): JSX.Element;
    defaultProps: {
        defaultOpen: boolean;
    };
};

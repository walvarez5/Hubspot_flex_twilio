import * as React from "react";
export declare const useStateRef: <A extends string | number | object | boolean | null | undefined | symbol>(defaultValue: A) => {
    state: A;
    dispatch: (val: A | ((a: A) => A)) => void;
    ref: React.MutableRefObject<A>;
};

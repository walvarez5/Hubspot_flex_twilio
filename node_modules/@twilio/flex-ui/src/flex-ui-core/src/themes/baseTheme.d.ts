import { Theme, Tokens } from "../components/theme";
import { DeepPartial } from "../types";
export interface Overrides {
    tokens?: DeepPartial<Tokens>;
}
/**
 * Creates a base theme for the colors passed.
 * @param  {Overrides} overrides An object which specifies the override tokens.
 * @param  {boolean} isLightTheme A boolean value used to set the contrast right.
 * @returns {Theme} The created theme
 * @ignore
 */
export declare function createTheme(overrides: Overrides, isLightTheme: boolean): Theme;

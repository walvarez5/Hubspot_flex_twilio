import { EmailDraftData } from "./components/channel/email/EmailEditor/EmailEditor.definitions";
export declare class EmailDraftHelper {
    private static readonly ErrorContext;
    private static readonly SAVED_CONVERSATIONS_KEY;
    private static readonly savedConversationSidsStorage;
    private emailDraftStorage;
    private conversationSid;
    constructor(conversationSid: string);
    saveDraft(draftData: EmailDraftData): void;
    retrieveDraft(): EmailDraftData | null;
    deleteDraft(): void;
    static removeDraftsOnLogout(): void;
    private static handleError;
}

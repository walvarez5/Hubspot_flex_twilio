declare class PreferenceDao {
    private getDb;
    get<T>(key: string): Promise<T>;
    add<T>(key: string, value: T): Promise<IDBValidKey>;
    put<T>(key: string, value: T): Promise<IDBValidKey>;
}
declare const preferenceDao: PreferenceDao;
export default preferenceDao;

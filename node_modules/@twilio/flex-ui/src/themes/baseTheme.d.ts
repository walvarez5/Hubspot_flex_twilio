import { PasteCustomCSS } from "@twilio-paste/customization";
import { Overrides } from "../flex-ui-core/src";
import { Theme } from "../components/theme";
/**
 * @category Theme
 * @private
 */
export declare function createTheme(overrides: Overrides, isLightTheme?: boolean): Theme;
export declare function createPasteTheme(theme: any): {
    [key: string]: PasteCustomCSS;
};

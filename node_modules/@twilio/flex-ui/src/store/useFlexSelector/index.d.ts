import { FlexState } from "../index";
/**
 * Wrapper for useSelector that guarantees FlexState in it's correct form
 * @param {Function} func selector
 * @param {Function} [equalityFn] equalityFn
 * @returns {any} selected
 * @example
 * import { useFlexSelector, FlexState } from "@twilio/flex-ui";
 * const workerName = useFlexSelector((state: FlexState) => state.flex.worker.attributes.full_name);
 */
export declare function useFlexSelector<S = FlexState, Selected = unknown>(func: (rootState: S) => Selected, equalityFn?: (left: Selected, right: Selected) => boolean): Selected;

import { TaskRouterTaskQueue } from "@twilio/flex-sdk";
import { Worker } from "@twilio/flex-sdk/taskrouter";
export declare const convertLiveQueryString: (query: string) => string;
export declare class WorkerDirectoryUtil {
    static transferWorkers: Worker[];
    static transferQueues: TaskRouterTaskQueue[];
    static latestWorker: boolean;
    static latestQueue: boolean;
}

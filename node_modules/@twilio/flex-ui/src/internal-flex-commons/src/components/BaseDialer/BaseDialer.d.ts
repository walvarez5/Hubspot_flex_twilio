import * as React from "react";
import type { ButtonThemeProps } from "../BaseDialpad/BaseDialpad.Components";
/**
 * @typedef Dialer.DialerCallback
 * @callback Dialer.DialerCallback
 * @param {string} phoneNumber phone number entered
 * @returns {void}
 * @since 2.0.0
 */
type DialerCallback = (phoneNumber: string) => void;
/**
 * @typedef  Dialer.DialerProps
 * @property {BaseDialer.DialerCallback} [onDial] - Callback when dialing action is submitted.
 * @property {boolean} [hideActions] - Identifies if the dialer actions are hidden.
 * @property {string} [defaultPhoneNumber] - Default phone number for dialer.
 * @property {BaseDialer.DialerCallback} [onPhoneNumberChange] - Callback when phone number is changed.
 * @property {boolean} [disabled] - Whether dialer is disabled.
 * @property {string} [defaultCountryAlpha2Code] - The default country alpha2 code.
 * @since 2.0.0
 */
export interface DialerProps {
    onDial: DialerCallback;
    hideActions?: boolean;
    defaultPhoneNumber?: string;
    onPhoneNumberChange?: DialerCallback;
    disabled?: boolean;
    defaultCountryAlpha2Code?: string;
}
interface BaseDialerProps extends DialerProps {
    templates: any;
    callButton: React.ReactNode;
    registerDialPadAction?: (callback: (payload: string) => void) => void;
    onKeyPress: (event: React.KeyboardEvent<HTMLInputElement>) => void;
    updateParentState: (phoneNumber: string) => void;
}
/**
 * @typedef Theme.DialerThemeProps
 * @property {ButtonThemeProps} CallButton Style for the dialer's Call Button
 * @since 2.0.0
 */
export interface DialerThemeProps {
    CallButton: ButtonThemeProps;
}
interface DialerState {
    phoneNumber: string;
    selectedCountryKey: string;
}
export declare class BaseDialer extends React.PureComponent<BaseDialerProps, DialerState> {
    static defaultProps: {
        defaultPhoneNumber: string;
        onPhoneNumberChange: () => void;
    };
    _isMounted: boolean;
    private inputRef;
    state: {
        phoneNumber: string;
        selectedCountryKey: string;
    };
    componentDidMount(): Promise<void>;
    componentWillUnmount(): void;
    render(): JSX.Element;
    private onRegisterDialPadAction;
    private updateListeners;
    private updateCountryCode;
    private onPhoneNumberReset;
    private onDialpadClick;
    private onDialpadUndo;
    private onDialerPhoneInputChange;
}
export {};

import { FC } from "react";
import { IQueue, BaseITask, IWorker } from "../../utils";
export interface BasePreTransferFormProps {
    worker?: IWorker;
    queue?: IQueue;
    task?: BaseITask;
    onCancelClick: () => void;
    setError: (error: string) => void;
    isNotesFFEnabled?: boolean;
    transferStarted: boolean;
    handleTransfer: (notes: string) => void;
}
export declare const BasePreTransferForm: FC<BasePreTransferFormProps>;

import React from "react";
export interface BaseProfileCardProps {
    fullName?: string;
    onClick?: () => void;
    imageUrl?: string;
    defaultAvatarIcon: React.ReactNode;
    templates: {
        ProfileCardAriaLabel: (props: {
            fullName: string;
        }) => string;
        AvatarAlt: () => string;
    };
}
export declare function BaseProfileCard({ templates, fullName, onClick, imageUrl, defaultAvatarIcon }: BaseProfileCardProps): JSX.Element;

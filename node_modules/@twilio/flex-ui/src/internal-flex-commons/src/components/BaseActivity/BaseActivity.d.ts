import type { EventEmitter } from "eventemitter3";
interface CurrentWorker {
    activityName?: string;
    activityDuration?: string;
    isAvailable?: boolean;
}
export interface ActivityProps {
    disabled: boolean;
    templates: any;
    activities: Map<string, any>;
    workerActivitySid: string;
    currentWorker?: CurrentWorker;
    setActivity: (sid: string) => Promise<void>;
    KeyboardShortcutManagerEventEmitter?: InstanceType<typeof EventEmitter>;
}
export declare function BaseActivity({ workerActivitySid, templates, activities, setActivity, disabled, currentWorker, KeyboardShortcutManagerEventEmitter }: ActivityProps): JSX.Element;
export {};

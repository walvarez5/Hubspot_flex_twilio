import React from "react";
export interface BaseUserControlsProps {
    defaultAvatarIcon: React.ReactNode;
    logoutButton: React.ReactNode;
    footer?: React.ReactNode;
    fullName?: string;
    workerImageUrl?: string;
    templates: {
        ProfileCardAriaLabel: (props: {
            fullName: string;
        }) => string;
        AvatarAlt: () => string;
        UserPopoverAriaLabel: (props: {
            fullName: string;
        }) => string;
        ProfileCardAvatarAltTextLabel: (props: {
            fullName: string;
        }) => string;
        ProfileCardAvatarAriaLabel: (props: {
            fullName: string;
        }) => string;
    };
}
export declare function BaseUserControls({ templates, fullName, workerImageUrl, defaultAvatarIcon, logoutButton, footer }: BaseUserControlsProps): JSX.Element;

import * as React from "react";
import { DynamicContentStore } from "../../../utils";
export declare enum CallCanvasDialpadChildrenKeys {
    digits = "digits",
    dialpad = "dialpad"
}
export interface DialpadState {
    digits: string;
}
export interface BaseCallCanvasDialpadProps<ParentsChildrenProps> {
    displayName: string;
    contentStore: DynamicContentStore;
    parentProps: React.PropsWithChildren<ParentsChildrenProps>;
    compact?: boolean;
    templates: any;
    dialpadState: DialpadState;
    onClick: (key: string) => void;
}

import * as React from "react";
interface MessageLoaderProps {
    messageList?: HTMLDivElement;
}
/**
 * This component is used to loading of historical data.
 *
 * @component
 * @category Components / Basic
 * @hideconstructor
 * @param {MessageLoaderProps} props
 * @since 2.0.0
 * @private
 */
export declare class MessageLoader extends React.PureComponent<MessageLoaderProps> {
    static readonly displayName = "MessageLoader";
    componentDidMount(): void;
    render(): JSX.Element;
}
export {};

/**
 * Add a parameter to call context.
 * @example
 * payload.to = addCallContextParameter(payload.to, "myContext=hello")
 * @private
 * @param existingRecipient
 * @param newContextParameter
 */
export declare const addCallContextParameter: (existingRecipient: string, newContextParameter: string) => string;

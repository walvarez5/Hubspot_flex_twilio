import { Client } from "@twilio/conversations";
export declare class ChatListener {
    static init(client: Client): void;
    private static handleConversationJoined;
    private static handleConversationUpdated;
}

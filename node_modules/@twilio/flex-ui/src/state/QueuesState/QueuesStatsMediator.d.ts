import { ChannelStatsAlert, MetricType, StableMetrics } from "../../core/FlexDataClient";
import { WorkerQueue, WorkerQueueChannelMetadata } from "./QueuesStateTypes";
type ChannelMetadataGetter = () => {
    [channelSid: string]: WorkerQueueChannelMetadata;
};
interface QueuesStatsMediatorConfig {
    tasksNowKey: string;
    tasks30minKey: string;
    tasksTodayKey: string;
    workerActivitiesStatsKey: string;
}
export declare class QueuesStatsMediator {
    private sid;
    private friendlyName;
    private tasksNow?;
    private tasksThirtyMinutes?;
    private tasksToday?;
    private agentsNow?;
    private workerStats?;
    private getChannelMetadata;
    private channels;
    private config;
    constructor(sid: string, friendlyName: string, getChannelMetadata: ChannelMetadataGetter, config: QueuesStatsMediatorConfig);
    applyQueueData(key: string, value: any): void;
    private saveChannelValue;
    private saveAgentMetrics;
    getQueueItem(): WorkerQueue;
    saveMetrics(metrics: StableMetrics, metricType: MetricType): void;
    private mapSlaMetrics;
    private mapTasksNow;
    private mapAgentMetrics;
    saveChannelData(channelStatsAlerts: ChannelStatsAlert[], metricType: MetricType): void;
    private prepareChannelData;
    dispatchUpdate: () => void;
}
export {};

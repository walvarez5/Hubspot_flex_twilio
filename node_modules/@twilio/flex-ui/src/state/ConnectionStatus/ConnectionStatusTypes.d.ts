import { SyncClient } from "twilio-sync/lib/client";
export declare const CONNECTION_STATUS_CHANGE = "FLEX_CONNECTION_STATUS_CHANGE";
/**
 * Connection status
 *
 * @interface ConnectionStatus
 * @property {number} timestampLastConnected - timestamp of last connection
 * @property {SyncClient.ConnectionState} connectionState - connection state of the sync client
 */
export interface ConnectionStatus {
    timestampLastConnected: number;
    connectionState: SyncClient.ConnectionState;
}
export type ConnectionStatusActionTypes = {
    type: typeof CONNECTION_STATUS_CHANGE;
    payload: SyncClient.ConnectionState;
};

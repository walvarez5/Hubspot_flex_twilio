import * as React from "react";
export declare const EmptyState: ({ heading, subheading, image, sideMargin }: {
    heading: string;
    subheading: string;
    image: React.ReactElement;
    sideMargin: boolean;
}) => JSX.Element;

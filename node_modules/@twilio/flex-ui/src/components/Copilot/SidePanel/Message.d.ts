import * as React from "react";
import { AIMessageVariants } from "../AIChatLog";
import { AiCopilotAssistApiReference } from "../../../core/FlexDataClient";
export declare const Message: ({ message, variant, author, imageURL, references, scrollerRef }: {
    message: string;
    variant: AIMessageVariants;
    author: string;
    imageURL?: string;
    references?: AiCopilotAssistApiReference[];
    scrollerRef?: React.MutableRefObject<null>;
}) => JSX.Element;

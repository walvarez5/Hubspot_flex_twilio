import * as React from "react";
import { ITask, IWorker } from "../../../models";
import { ThemeProps } from "../../theme";
export interface TransferClickPayloadChat {
    mode: "WARM" | "COLD";
}
export interface SelectedWorkerItemProps extends ThemeProps {
    worker: IWorker;
    task: ITask;
    onCancelClick: () => void;
    setError: (error: string) => void;
}
declare const SelectedWorkerItemThemed: React.FC<Pick<SelectedWorkerItemProps, "worker" | "task" | "onCancelClick" | "setError"> & {
    theme?: import("@emotion/react").Theme;
}>;
export { SelectedWorkerItemThemed as SelectedWorkerItem };

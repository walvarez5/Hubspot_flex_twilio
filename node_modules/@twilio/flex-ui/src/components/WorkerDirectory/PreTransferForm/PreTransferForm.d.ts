import React from "react";
import { ITask, IWorker, IQueue } from "../../../models";
interface PreTransferFormProps {
    worker?: IWorker;
    queue?: IQueue;
    task?: ITask;
    onCancelClick: () => void;
    setError: (error: string) => void;
}
export declare const PreTransferForm: React.FC<PreTransferFormProps>;
export {};

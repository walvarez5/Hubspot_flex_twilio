import * as React from "react";
import { IWorker } from "../../../models";
import { ThemeProps } from "../../theme";
export interface TransferClickPayload {
    mode: "WARM" | "COLD";
}
export interface WorkerItemProps extends ThemeProps {
    worker: IWorker;
    onTransferClick: (options: TransferClickPayload) => void;
    isWarmTransferEnabled?: boolean;
    isCallBasedTask: boolean;
}
declare const WorkerItemThemed: React.FC<Pick<WorkerItemProps, "worker" | "isWarmTransferEnabled" | "onTransferClick" | "isCallBasedTask"> & {
    theme?: import("@emotion/react").Theme;
}>;
export { WorkerItemThemed as WorkerItem };

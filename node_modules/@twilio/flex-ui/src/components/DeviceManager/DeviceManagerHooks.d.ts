import { useMenuState } from "@twilio-paste/core/menu";
type DeviceIds = {
    input: string;
    output: string;
};
export declare const useDeviceManager: (menu: ReturnType<typeof useMenuState>) => {
    menu: ReturnType<typeof useMenuState>;
    defaultDeviceIds: DeviceIds;
    selectDevice: (selectedDevice: MediaDeviceInfo) => Promise<void>;
    inputDevices: Array<MediaDeviceInfo>;
    outputDevices: Array<MediaDeviceInfo>;
};
export {};

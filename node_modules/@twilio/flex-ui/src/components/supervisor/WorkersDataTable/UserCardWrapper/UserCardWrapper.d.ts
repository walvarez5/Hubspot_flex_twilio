import * as React from "react";
import { IWorker } from "../../../../models/CommonModels";
export interface UserCardWrapperProps {
    worker: IWorker;
    selected: boolean;
    onClick?: (worker: IWorker) => void;
    showStatusInUserCard?: boolean;
}
export declare class UserCardWrapper extends React.PureComponent<UserCardWrapperProps> {
    handleClick: (e: React.SyntheticEvent) => void;
    render(): JSX.Element;
}

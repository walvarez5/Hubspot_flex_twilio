import { IWorker } from "../../../models";
/**
 * @typedef WorkerProfile.WorkerProfileProps
 * @property {IWorker} worker - Worker
 * @property {boolean} [editStatusPermission] - permission to edit the activity of other workers
 */
export type WorkerProfileProps = WorkerProfileImplProps;
export declare enum WorkerProfileChildrenKeys {
    avatar = "avatar",
    info = "info",
    availability = "availability"
}
export interface WorkerProfileImplProps {
    worker: IWorker;
    editStatusPermission?: boolean;
}
/**
 * Properties of Worker Profile.
 *
 * @typedef WorkerProfile.WorkerProfileChildrenProps
 * @property {IWorker} worker - Worker
 * @property {boolean} [editStatusPermission] - permission to edit the activity of other workers
 * @memberof WorkerProfile
 */
export interface WorkerProfileChildrenProps {
    worker: IWorker;
}

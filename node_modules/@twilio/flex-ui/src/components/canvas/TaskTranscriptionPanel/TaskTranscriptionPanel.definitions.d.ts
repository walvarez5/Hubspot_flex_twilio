import { DynamicContentStore } from "../../../flex-ui-core/src";
import { WorkerState } from "../../../state/WorkerState";
import { ThemeProps } from "../../theme";
import { TaskCanvasChildrenProps } from "../TaskCanvas/TaskCanvas.definitions";
/**
 * Properties of Task Transcription panel.
 * @typedef {Partial<TaskCanvasChildrenProps>} TaskTranscriptionPanel.TaskTranscriptionPanelProps
 * @property {string} [templateCode] - Represents template code.
 * @extends Partial<TaskCanvasChildrenProps>, ThemeProps
 */
export interface TaskTranscriptionPanelProps extends Partial<TaskCanvasChildrenProps>, ThemeProps {
    contentStore?: DynamicContentStore;
    templateCode?: string;
}
/**
 * Properties of children of Task Transcription panel.
 * @typedef {TaskTranscriptionPanelProps} TaskTranscriptionPanel.TaskTranscriptionPanelChildrenProps
 */
export interface TaskTranscriptionPanelChildrenProps extends TaskTranscriptionPanelProps {
    worker?: WorkerState;
}

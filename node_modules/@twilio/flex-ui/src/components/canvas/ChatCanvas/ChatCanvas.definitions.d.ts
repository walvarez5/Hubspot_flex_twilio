import { ConversationState } from "../../../flex-ui-core/src";
import { ITask } from "../../../models";
import { TaskChannelDefinition } from "../../../TaskChannelDefinition";
export interface ChatCanvasProps {
    task: ITask;
    conversation: ConversationState.ConversationState | undefined;
    hasMarkdownSupport: boolean;
    channelDefinition: TaskChannelDefinition;
    preventConversationUnload?: boolean;
}
export interface EmailCanvasProps extends ChatCanvasProps {
    task: ITask;
    conversation: ConversationState.ConversationState;
    participants: CombinedParticipants;
}
export interface CombinedParticipant extends Record<string, any> {
    conversationParticipantSid: string;
    universalParticipantSid: string;
    address: string;
}
export interface CombinedParticipants extends Record<string, CombinedParticipant[]> {
    to: CombinedParticipant[];
    cc: CombinedParticipant[];
}

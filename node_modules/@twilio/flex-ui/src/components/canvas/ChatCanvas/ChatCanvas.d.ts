import { ConversationState } from "../../../flex-ui-core/src";
import { FC } from "react";
import { ITask } from "../../../models";
import { ChatCanvasProps } from "./ChatCanvas.definitions";
export declare const conversationEndedReason: (task: ITask, conversation?: ConversationState.ConversationState) => string;
export declare const loadingParticipantReason: (task: ITask, trIsLoading: boolean, convIsLoading: boolean) => string;
export declare const ChatCanvas: FC<ChatCanvasProps>;

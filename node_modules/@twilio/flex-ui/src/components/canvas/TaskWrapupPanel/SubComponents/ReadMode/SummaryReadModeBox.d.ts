import * as React from "react";
export interface SummaryReadModeBoxProps {
    isLoading: boolean;
    finalSummary: string;
}
export declare const SummaryReadModeBox: React.FC<SummaryReadModeBoxProps>;

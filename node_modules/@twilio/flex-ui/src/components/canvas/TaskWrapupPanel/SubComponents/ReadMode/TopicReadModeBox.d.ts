import * as React from "react";
import { ChannelNoteCode, ChannelNoteCodes, DispositionCode, Topic } from "../../../../../core/FlexDataClient";
export interface TopicReadModeBoxProps {
    isLoading: boolean;
    codes: ChannelNoteCodes | undefined;
    topics?: Topic[];
    dispositionCodes?: DispositionCode[];
    dispositionCodeSid?: string;
    topicsFetchFailed?: boolean;
}
export interface CodeItemProps {
    code: ChannelNoteCode;
    length: number;
    currentIndex: number;
}
export declare const TopicReadModeBox: React.FC<TopicReadModeBoxProps>;

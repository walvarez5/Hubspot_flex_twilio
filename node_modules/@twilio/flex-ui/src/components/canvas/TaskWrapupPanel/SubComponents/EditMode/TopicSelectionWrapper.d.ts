import * as React from "react";
import { DispositionCode, Topic } from "../../../../../core/FlexDataClient";
import { DispositionCodesByParentSid, SelectedTopic, SubTopicsByParentSid } from "../../state/WrapupSummaryState";
export interface TopicSelectionWrapperProps {
    index: number;
    topics?: Topic[];
    selectedTopic: SelectedTopic;
    disableRemoveButton: boolean;
    onClickRemoveButton: (index: number) => void;
    onSelectedTopicChange: (index: number, topic: SelectedTopic) => void;
    dispositionCodesUncategorized?: DispositionCode[];
    topicsFetchFailed?: boolean;
    subTopicsByParentSid?: SubTopicsByParentSid;
    dispositionCodesByParentSid?: DispositionCodesByParentSid;
}
export declare const TopicSelectionWrapper: React.FC<TopicSelectionWrapperProps>;

import * as React from "react";
import { ITask } from "../../../../models";
interface EndCallMenuProps {
    task?: ITask;
    isLeaveDisabled: boolean;
    showResume?: boolean;
}
declare const EmailActionsMenu: React.FC<EndCallMenuProps>;
export default EmailActionsMenu;

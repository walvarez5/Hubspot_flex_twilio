import React from "react";
import { TaskRouterResource } from "../../types";
import { ReportName } from "../../utils";
interface DataFetcherProps {
    reportName?: ReportName;
    metricData: any;
    isError: boolean;
    isFetchMetricsLoading: boolean;
    teamMembers: TaskRouterResource[];
    workersList: TaskRouterResource[];
    isInsightsSection?: boolean;
    children: (data: TaskRouterResource[], isLoading: boolean) => React.ReactNode;
}
export declare const AgentListWrapper: ({ metricData, isError, isFetchMetricsLoading, reportName, teamMembers, workersList, isInsightsSection, children }: DataFetcherProps) => JSX.Element;
export {};

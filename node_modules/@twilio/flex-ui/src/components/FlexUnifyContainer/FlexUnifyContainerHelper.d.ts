import { Identifier, Profile } from "@twilio/flex-sdk";
export declare const setViewConfiguration: (response: any[]) => void;
export declare const getName: (profile: Profile) => string;
export declare const setConnectorOptionsConfiguration: (response: any[]) => void;
export declare const setConnectorSearchIdentifiers: (identifiers: Array<Identifier>) => void;
/**
 * Determines if a profile can be shown based on the profile status and the "show unidentified profiles" setting.
 * @param {boolean} showUnidentifiedEnabled - Flag indicating whether unidentified/"unknown" profiles should be shown.
 * @param {Profile.StatusEnum} [profileStatus] - The status of the profile.
 * @returns {boolean} - True if the profile can be shown, otherwise false.
 * @private
 * @since 2.11.0
 * @example
 * const canShow = canShowThisProfile(true, "unknown");
 * console.log(canShow); // true
 */
export declare const canShowThisProfile: (showUnidentifiedEnabled: boolean, profileStatus?: Profile.StatusEnum) => boolean;
export declare const addTaskSidToLocalStorage: (taskSid: string) => void;
export declare const removeTaskSidFromLocalStorage: (taskSid: string) => void;

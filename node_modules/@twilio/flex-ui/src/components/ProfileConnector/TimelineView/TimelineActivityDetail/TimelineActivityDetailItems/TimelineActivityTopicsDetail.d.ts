import { ChannelNote } from "../../timelineViewTypes";
interface TimelineActivityTopicsDetailProps {
    channelNote: ChannelNote;
    agentSid: string;
    channelSid: string;
    index: number;
}
export declare const TimelineActivityTopicsDetail: {
    ({ channelNote, agentSid, channelSid, index }: TimelineActivityTopicsDetailProps): JSX.Element;
    displayName: string;
};
export {};

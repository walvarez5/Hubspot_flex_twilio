import { ITask } from "../../../../models/CommonModels";
export declare enum TimelineEventName {
    TIMELINE_VIEW_EVENT = "Timeline Component Viewed",
    TIMELINE_TAB_CLICKED_EVENT = "Timeline Tab Clicked",
    TIMELINE_DETAILS_CLICKED = "Timeline Activity Details Clicked",
    TIMELINE_VIEW_MORE_CLICKED = "Timeline View More Clicked",
    TIMELINE_MANUAL_REFRESH_CLICKED = "Timeline Manual Refresh Button Clicked"
}
export type TimelineEventProperties = {
    eventName: TimelineEventName;
    task?: ITask;
    profileConnectSid?: string;
    isRenderedInTab?: boolean;
};
/**
 * Tracks a timeline event and sends telemetry data to MixPanel.
 * @param {TimelineEventProperties} eventProps - The properties of the event to track.
 * @example
 * // Example usage:
 * // trackTimelineEvent({eventName: TimelineEventName.TIMELINE_VIEW_EVENT, task, "YI1234567890"});
 */
export declare const trackTimelineEvent: (eventProps: TimelineEventProperties) => void;

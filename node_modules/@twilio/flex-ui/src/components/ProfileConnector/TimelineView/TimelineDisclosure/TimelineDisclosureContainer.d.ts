import { ITask } from "../../../../models";
import { ActivityDetailModel, ActivityModelTypes, TimelineActivityDetailItem } from "../timelineViewTypes";
interface TimelineDisclosureContainerProps {
    activity: ActivityModelTypes;
    isLastItem: boolean;
    elementKey: string;
    profileConnectSid: string;
    task?: ITask;
    isRenderedInTab?: boolean;
    configDrivenDetailItems: (eventName: string, details?: ActivityDetailModel[]) => TimelineActivityDetailItem[] | undefined;
}
declare const TimelineDisclosureContainer: {
    ({ activity, elementKey, isLastItem, profileConnectSid, task, isRenderedInTab, configDrivenDetailItems }: TimelineDisclosureContainerProps): JSX.Element;
    displayName: string;
    defaultProps: {
        task: any;
        isRenderedInTab: any;
    };
};
export { TimelineDisclosureContainer };

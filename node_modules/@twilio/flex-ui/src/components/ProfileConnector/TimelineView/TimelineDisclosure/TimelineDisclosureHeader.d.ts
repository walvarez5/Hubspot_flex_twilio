import { ActivityModelTypes } from "../timelineViewTypes";
interface TimelineDisclosureHeaderProps {
    activity: ActivityModelTypes;
}
declare const TimelineDisclosureHeader: {
    ({ activity }: TimelineDisclosureHeaderProps): JSX.Element;
    displayName: string;
};
export { TimelineDisclosureHeader, TimelineDisclosureHeaderProps };

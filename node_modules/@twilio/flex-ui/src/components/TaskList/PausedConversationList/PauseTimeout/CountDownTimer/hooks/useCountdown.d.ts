interface CountdownProps {
    remainingTime?: number;
    day?: boolean;
    hour?: boolean;
    minute?: boolean;
    seconds?: boolean;
    callback: () => void;
}
/**
 * Calculate the remaining time in days, hours, minutes, seconds.
 * @param {number} time - The total time in milliseconds.
 * @param {boolean} [day] - Whether to display days.
 * @param {boolean} [hour] - Whether to display hours.
 * @param {boolean} [minute] - Whether to display minutes.
 * @param {boolean} [seconds] - Whether to display seconds.
 * @returns {{days: number, hours: number, minutes: number, seconds: number}} The remaining time in days, hours, minutes, and seconds.
 * @since 2.11.0
 * @example
 * const time = calculateTime(3600000, true, true, true, true);
 * console.log(time); // { days: 0, hours: 1, minutes: 0, seconds: 0 }
 */
export declare function calculateTime(time: number, day?: boolean, hour?: boolean, minute?: boolean, seconds?: boolean): {
    days: number;
    hours: number;
    minutes: number;
    seconds: number;
};
/**
 * useCountdown
 * - Sets up an interval to decrement the remaining time.
 * - Returns the updated { days, hours, minutes, seconds } object.
 *
 * The smallest granularity you pass (seconds, minute, hour) determines
 * how frequently the hook updates.
 *
 * @private
 *
 * @param {object} root0 - The countdown properties.
 * @param {number} [root0.remainingTime] - The total milliseconds left.
 * @param {boolean} [root0.day] - Whether to display days.
 * @param {boolean} [root0.hour] - Whether to display hours.
 * @param {boolean} [root0.minute] - Whether to display minutes.
 * @param {boolean} [root0.seconds] - Whether to display seconds.
 * @param {Function} root0.callback - Called when time <= 0.
 *
 * @since 2.11.0
 * @returns {{days: number, hours: number, minutes: number, seconds: number}} The remaining time in days, hours, minutes, and seconds.
 * @example
 *
 * const { days, hours, minutes, seconds } = useCountdown({ remainingTime: 3600000, day: true, hour: true, minute: true, seconds: true, callback: () => console.log('Time is up!') });
 * console.log(days, hours, minutes, seconds); // 0 1 0 0
 */
declare function useCountdown({ remainingTime, day, hour, minute, seconds, callback }: CountdownProps): {
    days: number;
    hours: number;
    minutes: number;
    seconds: number;
};
export default useCountdown;

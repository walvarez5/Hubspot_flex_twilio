import { Theme } from "../../../../../theme";
declare function useLabelTemplate(status_timeout: string | null, conversationSid: string, isPauseStatusTimeOut: boolean, theme: Theme): {
    labelComponent: JSX.Element;
    color: string;
};
export default useLabelTemplate;

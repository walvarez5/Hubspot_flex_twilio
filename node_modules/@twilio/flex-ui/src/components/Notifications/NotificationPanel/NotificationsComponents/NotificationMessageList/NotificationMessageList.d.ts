import { MarkMessageAsRead } from "../../../NotificationHooks/NotificationPanelHooks";
import { NotificationFilterState, NotificationMessage as NotificationMessageType } from "../../Notifications.definitions";
interface NotificationMessageListProps {
    messages: NotificationMessageType[];
    filter: NotificationFilterState;
    updateFilter: (value: number) => void;
    fetchData: () => void;
    markAsRead: MarkMessageAsRead;
    hasMore: boolean;
    loading: boolean;
    isFetched: boolean;
}
export declare enum NotificationSection {
    Today = "Today",
    Yesterday = "Yesterday",
    Older = "Older"
}
declare function NotificationMessageList(props: Readonly<NotificationMessageListProps>): JSX.Element;
export default NotificationMessageList;

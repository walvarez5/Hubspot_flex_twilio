let crypto,URLSearchParams,FormData;_eb6‍.x([["default",()=>_eb6‍.o]]);_eb6‍.w("crypto",[["default",["crypto"],function(v){crypto=v}]]);_eb6‍.w("./classes/URLSearchParams.js",[["default",["URLSearchParams"],function(v){URLSearchParams=v}]]);_eb6‍.w("./classes/FormData.js",[["default",["FormData"],function(v){FormData=v}]]);



const ALPHA = 'abcdefghijklmnopqrstuvwxyz'

const DIGIT = '0123456789';

const ALPHABET = {
  DIGIT,
  ALPHA,
  ALPHA_DIGIT: ALPHA + ALPHA.toUpperCase() + DIGIT
}

const generateString = (size = 16, alphabet = ALPHABET.ALPHA_DIGIT) => {
  let str = '';
  const {length} = alphabet;
  const randomValues = new Uint32Array(size);
  crypto.randomFillSync(randomValues);
  for (let i = 0; i < size; i++) {
    str += alphabet[randomValues[i] % length];
  }

  return str;
}


_eb6‍.d({
  isNode: true,
  classes: {
    URLSearchParams,
    FormData,
    Blob: typeof Blob !== 'undefined' && Blob || null
  },
  ALPHABET,
  generateString,
  protocols: [ 'http', 'https', 'file', 'data' ]
});
